# Pipeline Team — Brand Guidelines

_Generated by brandkit-generator. Do not hand-edit — regenerate from `brandkit.config.yaml`._

## The mark

The mark is the CI/CD loop with the highlighted **P**. Ship these variants:

| Variant | File | Use |
|---|---|---|
| Duotone (light) | `logo/mark-duotone-light.svg` | Default, on light backgrounds |
| Duotone (dark) | `logo/mark-duotone-dark.svg` | On dark backgrounds |
| Mono | `logo/mark-mono.svg` | Single-color contexts — inherits `currentColor` |
| On accent | `logo/mark-on-accent.svg` | White knockout, on the brand blue |

**Small sizes use the mark only** — never the full lockup below 24px. Favicons floor at 16px.

## Lockups

Pair the mark with the wordmark **Pipeline Team** (Space Grotesk 700, the first word bold and the rest regular).

- **Horizontal** — `logo/lockup-horizontal-{light,dark}.svg` — default for headers, docs, README.
- **Vertical** — `logo/lockup-vertical-{light,dark}.svg` — for square/centered placements.
- **Wordmark** — `logo/wordmark-{light,dark}.svg` — the logotype alone, no mark, for text-only contexts.

Alignment is optical: the wordmark cap-height is centered on the mark.

## Clear space & minimum size

- **Clear space:** keep a margin of **0.5 × X** on all sides, where **X = the mark height**. This ratio holds at every size — never use a fixed pixel value.
- **Lockup gap:** the mark-to-wordmark gap is **0.3 × X**.
- **Minimum size:** 24px for the mark in digital use; 16px favicon floor.

## Do / Don't

**Do**
- Use the provided variants and tokens.
- Preserve the mark's aspect ratio; scale uniformly.
- Give the mark its clear space.

**Don't**
- Stretch, skew, rotate, or add effects to the mark.
- Recolor the mark outside the approved palette.
- Place the full lockup below the minimum size — use the mark.
- Put the mark on a low-contrast background.

## Color

Palette derived perceptually (OKLCH) from the primary `#0969DA`. Usage follows ~**60/30/10** (surface / secondary / accent). Every foreground/background pairing meets **WCAG AA** (lowest audited ratio 4.76:1). Full ramps and per-pairing audit live in `brand-spec.md`; machine-readable tokens in `tokens/tokens.json` (DTCG) and `tokens/tailwind.theme.css`.

### Light theme

| Token | Value |
|---|---|
| `bg` | `#FFFFFF` |
| `surface` | `#e7eaee` |
| `fg` | `#0D1117` |
| `muted-fg` | `#676d75` |
| `border` | `#d6d9de` |
| `ring` | `#0969DA` |
| `primary` | `#0969DA` |
| `primary-fg` | `#f4f5f8` |
| `accent` | `#0969DA` |
| `accent-fg` | `#f4f5f8` |

### Dark theme

| Token | Value |
|---|---|
| `bg` | `#0D1117` |
| `surface` | `#2a2d32` |
| `fg` | `#F0F6FC` |
| `muted-fg` | `#9ba0a7` |
| `border` | `#3c4147` |
| `ring` | `#4493F8` |
| `primary` | `#4493F8` |
| `primary-fg` | `#1b1e21` |
| `accent` | `#4493F8` |
| `accent-fg` | `#1b1e21` |

## Typography

| Role | Family | Weight |
|---|---|---|
| Display / headings | Space Grotesk | 700 |
| Body / subtitle | IBM Plex Sans | 400 |
| Code / mono | JetBrains Mono | 400 |

Modular type scale, ratio **1.25**, base **16px**.

## Applications

| Surface | Size | Use |
|---|---|---|
| favicon | 512×512 | Browser favicon (full mark, white on a rounded accent tile) |
| apple-touch-icon | 180×180 | iOS home-screen icon (rounded, on white) |
| maskable | 512×512 | PWA maskable icon (80% safe zone, on accent) |
| avatar | 400×400 | Social avatar (circle-safe, white mark on accent) |
| og-card | 1200×630 | Open Graph / social card (1200×630) |
| header | 1200×240 | Site / docs header |
| footer | 1200×160 | Site / docs footer (on surface) |
| banner | 1200×320 | Wide banner (dark theme) |

Every generated asset is indexed in `manifest.json` with dimensions and intended use.
